from django.contrib import admin
from registration.models import Profile, Game, Tournament

admin.site.register(Profile)
admin.site.register(Game)
admin.site.register(Tournament)

# Register your models here.
